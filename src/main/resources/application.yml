spring:
  application:
    name: MS-Mensajeria

  datasource:
    url: jdbc:mysql://localhost:3306/mensajeria_db
    username: root
    password: Duoc.2026
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8087